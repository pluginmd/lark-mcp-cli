{
  "_comment": "Merge into ~/.cursor/mcp.json. Cursor reloads on config change; no restart needed.",
  "mcpServers": {
    "lark-cli": {
      "command": "<BINARY-PATH>",
      "args": ["mcp", "serve"],
      "env": {
        "NO_COLOR": "1"
      }
    }
  }
}
