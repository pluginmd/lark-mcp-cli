{
  "_comment": "Merge into ~/.cline/mcp_settings.json (VS Code Cline extension).",
  "mcpServers": {
    "lark-cli": {
      "command": "<BINARY-PATH>",
      "args": ["mcp", "serve"],
      "env": {
        "NO_COLOR": "1"
      },
      "disabled": false,
      "autoApprove": []
    }
  }
}
