{
  "_comment": "Merge this entry into your existing mcpServers block. Path: ~/Library/Application Support/Claude/claude_desktop_config.json on macOS.",
  "mcpServers": {
    "lark-cli": {
      "command": "<BINARY-PATH>",
      "args": ["mcp", "serve"],
      "env": {
        "NO_COLOR": "1"
      }
    }
  }
}
