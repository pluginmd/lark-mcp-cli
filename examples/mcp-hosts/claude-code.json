{
  "_comment": "Merge into ~/.claude.json under mcpServers. Or use the CLI: claude mcp add lark-cli <BINARY-PATH> mcp serve",
  "mcpServers": {
    "lark-cli": {
      "type": "stdio",
      "command": "<BINARY-PATH>",
      "args": ["mcp", "serve"],
      "env": {
        "NO_COLOR": "1"
      }
    }
  }
}
